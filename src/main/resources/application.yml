server:
  port: 8080

spring:
  thymeleaf:
    prefix: classpath:/templates/
    cache: false
    mode: HTML
    encoding: UTF-8
    suffix: .html
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB
  profiles:
    active: dev
